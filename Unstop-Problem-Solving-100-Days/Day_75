import heapq

n, k = map(int, input().split())
arr = list(map(int, input().split()))

heap = [-x for x in arr]
heapq.heapify(heap)

ans = []
for _ in range(k):
    ans.append(str(-heapq.heappop(heap)))

print(*ans)